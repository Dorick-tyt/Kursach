[project]
name = "pythonkurcovaya"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.2",
    "requests>=2.33.1",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
    "pytest>=9.0.2",
    "ruff>=0.15.9",
]
test = [
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
]
